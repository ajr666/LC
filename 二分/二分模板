区间A, B

bool checkA(int x) {/* ... */} // 检查x是否落在A区间
bool checkB(int x) {/* ... */} // 检查x是否落在B区间

找区间B的左端点:
int bsearch_1(int l, int r)
{
    while (l < r)
    {
        int mid = l + r >> 1;
        if (checkB(mid)) r = mid;
        else l = mid + 1;
    }
    return l;
}

找区间A的右端点:
int bsearch_2(int l, int r)
{
    while (l < r)
    {
        int mid = l + r + 1 >> 1;
        if (checkA(mid)) l = mid;
        else r = mid - 1;
    }
    return l;
}

